# Colab-friendly pip requirements derived from environment.yml.
# This is intentionally a practical subset, not a 1:1 export of Conda/CUDA packages.
#
# Notes for Google Colab:
# - Colab already ships with PyTorch. Install this file after the runtime starts.
# - If you need to reinstall PyTorch to match a specific CUDA build, do that first.
# - `rdkit-pypi` is used here because it installs more reliably on Colab than Conda `rdkit`.
# - PyG extension wheels such as `torch-scatter` are version-specific and should be
#   installed separately from https://data.pyg.org after checking the Colab torch version.

lightning==2.0.5
pytorch-lightning==2.0.4
torchmetrics==0.11.4

transformers==4.36.2
datasets==2.16.1
huggingface-hub==0.20.1
hf-transfer==0.1.8
accelerate==0.25.0
peft==0.7.1
bitsandbytes==0.39.0
safetensors==0.4.1
tokenizers==0.15.0
sentence-transformers==2.2.2
sentencepiece==0.1.99

deepspeed==0.12.6
wandb==0.14.0
tensorboard==2.18.0

torch-geometric==2.3.0
ogb==1.3.6
networkx==2.8.4
scipy==1.17.1
scikit-learn==1.2.2
# numpy==1.24.3
# pandas==1.5.2
# pyarrow==11.0.0
# pyarrow-hotfix==0.6

rdkit
pympler==1.1
test-tube==0.7.5
# tqdm==4.65.0
# pyyaml==6.0
# nltk==3.8.1
# imageio==2.36.1
# gitpython==3.1.30
# requests==2.29.0
